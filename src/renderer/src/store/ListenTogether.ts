// Stub: listen together feature removed
import { defineStore } from 'pinia'
export const useListenTogetherStore = defineStore('listenTogether', {
  state: () => ({
    isInRoom: false,
    current: { song: null as any }
  }),
  getters: {},
  actions: {}
})
